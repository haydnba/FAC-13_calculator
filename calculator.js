// JS Calculator Module...

const calculate = x => {

	const add = y => x + y;
	const subtract = y => x - y;
	const multiply = y => x * y;
	const divide = y => x / y;
	const equals = () => result;

	return {
    	value: x,
    	add: add,
		subtract: subtract,
		multiply: multiply,
		divide: divide
	};

};


class Calculation {

	constructor() {
		
	}

}


class Calculation {

		constructor(input) {
			this.input = calculate(parseFloat(input));	
		}

		evaluate(output) {
			return calculate(this.input[ouput])

			return this.input.value[output];
		}

	}